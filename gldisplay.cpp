#include "gldisplay.h"


GLDisplay::GLDisplay(QWidget *parent) :
    QGLWidget(parent)
{
    /*vid = VideoCapture(0);
    if (!vid.isOpened()){
        exit(1);
    }
    nbFrame = 0;*/

}

void GLDisplay::paintGL()
{
    /*nbFrame++,
    vid.read(frame);

    update();*/

}

